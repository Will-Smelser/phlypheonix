<?php
class IncomingController extends AppController {
	var $uses = array();
	
	/**
	 * Handles the sharing of incoming links
	 * @param unknown_type $referrer The user id of the individual who shared
	 */
	function shared($referrer){
		
	}
}